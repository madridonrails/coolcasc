<%unless order_line.blank?%>
  <% @order_line = order_line %>
  <div id="order_line-information-<%= order_line.id %>" class="span-24 miniform"> 
    <% form_for @order_line,
      :url => {:action => :update, :id => order_line.id},
      :method => :put,
      :html => {
                :multipart => true
      } do |f| %> 

        <%= render :partial => 'form', :locals => {:order_line => @order_line} %>
      <!--%= link_to image_tag('delete.png', :alt => t('general.delete'), :title => t('general.delete') ), order_order_line_url(@order_line), :method => 'delete', :confirm => t('general.are_you_sure') %-->

      <input type="hidden" name="order_id" value="<%= @order_line.order_id %>"/>
      <input type="hidden" name="order_line_id" value="<%= @order_line.id %>"/>
    <% end %>

  </div>
<% end %>
