<% unless @order_line.nil? %>
  <div class="span-24 miniform"> 
    <% form_for @order_line, 
      :url => order_order_lines_path(@order), 
      :html => {
                :id => 'new-order-line-form', 
                :multipart => true
               } do |f| %>
                 <%= render :partial => 'form', :object => f %>
    <% end %>
  </div>
<%end%>
